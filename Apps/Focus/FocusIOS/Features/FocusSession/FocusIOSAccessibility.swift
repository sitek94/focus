/// Stable accessibility identifiers for the Focus iOS shell UI smokes.
enum FocusIOSAccessibility {
  /// Root scene container.
  static let root = "focus.ios.root"
  /// Brand / product name.
  static let brand = "focus.ios.brand"
  /// Non-released shell status placeholder copy.
  static let status = "focus.ios.status"
}
