/// Stable accessibility identifiers for the Focus iOS UI smokes.
enum FocusIOSAccessibility {
  /// Root scene container.
  static let root = "focus.ios.root"
  /// Brand / product name.
  static let brand = "focus.ios.brand"
  /// Marketing version, build number, and short git commit.
  static let version = "focus.ios.version"
}
